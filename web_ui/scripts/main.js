document.addEventListener("DOMContentLoaded", () => {
    console.log("xpod UI loaded and ready.");
    // This is where I will establish the WebSocket connection to the Rust server
    // to receive real-time telemetry from the Vector clank API.
});